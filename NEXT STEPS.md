1. status strip isnt showing the color properly status-strip, i want the status of the things to be shown by making the load once and show till the server shutdown type of state 

2. log_service.log_frontend(f"Rendering {filename} -> {out_name} ({params['ratio_key']})", source="editor"), here i want more logs like add 3 - 4 more logs for each of the rendering of the things for a single clip, keep 3 - 4 logs for each clips, mentioning what happens inside by adding logs in ffmpeg_service file itself or its parent to track about all the things. also show the progress bar to the frontend by updating the width based on the progress that is being made for each video that is in the queue. update the editor section too to show the logs with progress bar for the editor. should update the width of the bar based on the progress done.